name: Kalern
options: # Some general settings for the projectproject.yml
  createIntermediateGroups: true # If the folders are nested, also nest the groups in Xcode
  indentWidth: 4
  tabWidth: 4
  bundleIdPrefix: "com.yossansr"
configs:
  Debug: debug
  integ: debug
  test: debug
  Release: release
targets:
  Kalern:
    type: application
    platform: iOS
    deploymentTarget: "14.5"
    sources:  
      - path: Kalern